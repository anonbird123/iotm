void setup(){
  pinMode(A1, INPUT);
  pinMode(4, OUTPUT);
  Serial.begin(9600);
}

void loop(){
  int temp = analogRead(A1);
  Serial.println(temp);

  if(temp>300){
    digitalWrite(4, HIGH);
  }
  else{
    digitalWrite(4, LOW);
  }
  delay(3000);
}
